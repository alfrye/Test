categories:
- Logic
questions:
- variable: ingress.enabled
  default: true
  description: "Expose fiddlepad using Load Balancer - ingress"
  type: boolean
  group: "Fiddlepad Web Server"
  label: Expose Fiddlepad to load balancer
  show_subquestion_if: true
  required: true
  subquestions:
  - variable: server.ingress.hosts[0]
    default: "xip.io"
    description: "Fiddlepad server ingress name"
    type: hostname
    required: true
    label: Hostname
    

